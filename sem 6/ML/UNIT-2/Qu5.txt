import matplotlib.pyplot as plt
from collections import Counter

data = ['Apple', 'Banana', 'Orange', 'Apple', 'Banana', 'Banana', 'Orange', 'Apple']

count=Counter(data)

fruits=list(count.keys())
values=list(count.values())

plt.bar(fruits,values,color="Blue")
plt.show()